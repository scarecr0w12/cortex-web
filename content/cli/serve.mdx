# `cortex serve`

The `cortex serve` command manages serve operations in CortexPrism.

## Usage

```bash
cortex serve [options]
```

## Options

| Option | Description |
|--------|-------------|
| `--help` | Show help for this command |

## Examples

```bash
cortex serve --help
```
