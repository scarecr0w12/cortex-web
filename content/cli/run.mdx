# `cortex run`

The `cortex run` command manages run operations in CortexPrism.

## Usage

```bash
cortex run [options]
```

## Options

| Option | Description |
|--------|-------------|
| `--help` | Show help for this command |

## Examples

```bash
cortex run --help
```
