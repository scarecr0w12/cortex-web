# `cortex setup`

The `cortex setup` command manages setup operations in CortexPrism.

## Usage

```bash
cortex setup [options]
```

## Options

| Option | Description |
|--------|-------------|
| `--help` | Show help for this command |

## Examples

```bash
cortex setup --help
```
