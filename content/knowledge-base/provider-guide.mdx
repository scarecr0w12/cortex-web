# Provider Guide

Step-by-step setup guides for all supported LLM providers.

## Anthropic Claude

```bash
# 1. Get your API key from https://console.anthropic.com
# 2. Configure in config.json:
```

```json
{
  "anthropic": { "kind": "anthropic", "model": "claude-sonnet-4-5", "apiKey": "sk-ant-..." }
}
```

Recommended models: `claude-sonnet-4-5`, `claude-haiku-4-5`, `claude-opus-4-5`

## OpenAI

```bash
# 1. Get your API key from https://platform.openai.com/api-keys
# 2. Configure:
```

```json
{
  "openai": { "kind": "openai", "model": "gpt-4o", "apiKey": "sk-..." }
}
```

Recommended models: `gpt-4o`, `gpt-4o-mini`, `gpt-4-turbo`

## Google Gemini

```bash
# 1. Get your API key from https://aistudio.google.com/apikey
# 2. Configure:
```

```json
{
  "google": { "kind": "google", "model": "gemini-2.0-flash", "apiKey": "..." }
}
```

Recommended models: `gemini-2.0-flash`, `gemini-2.5-pro`

## Mistral AI

```bash
# 1. Get your API key from https://console.mistral.ai
# 2. Configure:
```

```json
{
  "mistral": { "kind": "mistral", "model": "mistral-large-latest", "apiKey": "..." }
}
```

## Groq

```bash
# 1. Get your API key from https://console.groq.com
# 2. Configure (OpenAI-compatible):
```

```json
{
  "groq": { "kind": "groq", "model": "llama-3.3-70b-versatile", "apiKey": "gsk_..." }
}
```

## DeepSeek

```bash
# 1. Get your API key from https://platform.deepseek.com
# 2. Configure (OpenAI-compatible):
```

```json
{
  "deepseek": { "kind": "deepseek", "model": "deepseek-chat", "apiKey": "sk-..." }
}
```

## OpenRouter

```bash
# 1. Get your API key from https://openrouter.ai/keys
# 2. Configure (OpenAI-compatible):
```

```json
{
  "openrouter": { "kind": "openrouter", "model": "openai/gpt-4o", "apiKey": "..." }
}
```

OpenRouter gives access to 200+ models from various providers.

## xAI (Grok)

```bash
# 1. Get your API key from https://x.ai/api
# 2. Configure (OpenAI-compatible):
```

```json
{
  "xai": { "kind": "xai", "model": "grok-2-latest", "apiKey": "..." }
}
```

## Together AI

```bash
# 1. Get your API key from https://api.together.ai/settings/api-keys
# 2. Configure (OpenAI-compatible):
```

```json
{
  "together": { "kind": "together", "model": "meta-llama/Llama-3.3-70B-Instruct-Turbo", "apiKey": "..." }
}
```

## AWS Bedrock

```bash
# 1. Set up AWS credentials with Bedrock access
# 2. Configure:
```

```json
{
  "bedrock": { "kind": "bedrock", "model": "anthropic.claude-3-5-sonnet-20240620-v1:0", "apiKey": "AKIA...", "secretKey": "...", "baseUrl": "us-east-1" }
}
```

## Cohere

```bash
# 1. Get your API key from https://dashboard.cohere.com/api-keys
# 2. Configure:
```

```json
{
  "cohere": { "kind": "cohere", "model": "command-r-plus", "apiKey": "..." }
}
```

## Ollama (Local)

```bash
# 1. Install Ollama from https://ollama.com
# 2. Pull a model:
ollama pull llama3.2

# 3. Configure:
```

```json
{
  "ollama": { "kind": "ollama", "model": "llama3.2", "baseUrl": "http://localhost:11434" }
}
```

Ollama runs entirely locally — no API key needed.

## Cerebras

```bash
# 1. Get your API key from https://cloud.cerebras.ai
# 2. Configure (OpenAI-compatible):
```

```json
{
  "cerebras": { "kind": "cerebras", "model": "llama3.1-8b", "apiKey": "..." }
}
```

## Fireworks AI

```bash
# 1. Get your API key from https://fireworks.ai/api-keys
# 2. Configure (OpenAI-compatible):
```

```json
{
  "fireworks": { "kind": "fireworks", "model": "accounts/fireworks/models/llama-v3p1-70b-instruct", "apiKey": "..." }
}
```

## Perplexity

```bash
# 1. Get your API key from https://docs.perplexity.ai
# 2. Configure (OpenAI-compatible):
```

```json
{
  "perplexity": { "kind": "perplexity", "model": "llama-3.1-sonar-large-128k-online", "apiKey": "..." }
}
```

Perplexity supports `searchRecencyFilter` for month/week/day/hour search freshness.

## NVIDIA NIM

```bash
# 1. Get your API key from https://build.nvidia.com
# 2. Configure (OpenAI-compatible):
```

```json
{
  "nvidia": { "kind": "nvidia", "model": "meta/llama-3.1-405b-instruct", "apiKey": "..." }
}
```

## Moonshot (Kimi)

```bash
# 1. Get your API key from https://platform.moonshot.cn
# 2. Configure (OpenAI-compatible):
```

```json
{
  "moonshot": { "kind": "moonshot", "model": "moonshot-v1-8k", "apiKey": "..." }
}
```

## Novita AI

```bash
# 1. Get your API key from https://novita.ai
# 2. Configure (OpenAI-compatible):
```

```json
{
  "novita": { "kind": "novita", "model": "meta-llama/llama-3.1-8b-instruct", "apiKey": "..." }
}
```

## LM Studio (Local)

```bash
# 1. Install LM Studio from https://lmstudio.ai
# 2. Load a model and start the local server
# 3. Configure:
```

```json
{
  "lmstudio": { "kind": "lmstudio", "model": "local-model", "baseUrl": "http://localhost:1234/v1" }
}
```

LM Studio runs entirely locally — no API key needed.

## LiteLLM (Proxy)

```bash
# 1. Set up LiteLLM proxy from https://docs.litellm.ai
# 2. Configure (OpenAI-compatible):
```

```json
{
  "litellm": { "kind": "litellm", "model": "gpt-4o", "baseUrl": "http://localhost:4000/v1", "apiKey": "..." }
}
```

## Hugging Face

```bash
# 1. Get your API token from https://huggingface.co/settings/tokens
# 2. Configure (OpenAI-compatible):
```

```json
{
  "huggingface": { "kind": "huggingface", "model": "meta-llama/Llama-3.1-8B-Instruct", "apiKey": "hf_..." }
}
```

## Alibaba (Qwen)

```bash
# 1. Get your API key from https://dashscope.console.aliyun.com
# 2. Configure (OpenAI-compatible):
```

```json
{
  "alibaba": { "kind": "alibaba", "model": "qwen-max", "apiKey": "sk-..." }
}
```

## Venice AI

```bash
# 1. Get your API key from https://venice.ai/settings/api
# 2. Configure (OpenAI-compatible):
```

```json
{
  "venice": { "kind": "venice", "model": "llama-3.3-70b", "apiKey": "..." }
}
```

Venice AI is privacy-focused with no logging.

## DeepInfra

```bash
# 1. Get your API key from https://deepinfra.com/dash/api_keys
# 2. Configure (OpenAI-compatible):
```

```json
{
  "deepinfra": { "kind": "deepinfra", "model": "meta-llama/Llama-3.3-70B-Instruct", "apiKey": "..." }
}
```

DeepInfra offers serverless GPU inference with competitive pricing.

## Hyperbolic

```bash
# 1. Get your API key from https://app.hyperbolic.xyz
# 2. Configure (OpenAI-compatible):
```

```json
{
  "hyperbolic": { "kind": "hyperbolic", "model": "deepseek-ai/DeepSeek-V3", "apiKey": "..." }
}
```

Hyperbolic provides ~80% cheaper inference than competing cloud providers.

## MiniMax

```bash
# 1. Get your API key from https://platform.minimaxi.com
# 2. Configure (OpenAI-compatible):
```

```json
{
  "minimax": { "kind": "minimax", "model": "MiniMax-M3", "apiKey": "..." }
}
```

MiniMax M3 achieves 80.5% on SWE-bench at $0.30/$1.20 per 1M tokens.

## Zhipu (GLM)

```bash
# 1. Get your API key from https://open.bigmodel.cn
# 2. Configure (OpenAI-compatible):
```

```json
{
  "zhipu": { "kind": "zhipu", "model": "glm-4-flash", "apiKey": "..." }
}
```

Zhipu offers a free tier with the `glm-4-flash` model.

## Replicate

```bash
# 1. Get your API token from https://replicate.com/account/api-tokens
# 2. Configure:
```

```json
{
  "replicate": { "kind": "replicate", "model": "meta/meta-llama-3.1-405b-instruct", "apiKey": "r8_..." }
}
```

Replicate uses a predictions-based API with SSE streaming.

## Cloudflare Workers AI

```bash
# 1. Get your Cloudflare Account ID and API token from https://dash.cloudflare.com
# 2. Configure:
```

```json
{
  "cloudflare": { "kind": "cloudflare", "model": "@cf/meta/llama-3.1-8b-instruct", "apiKey": "...", "accountId": "..." }
}
```

Cloudflare Workers AI runs inference at the edge. Requires Account ID + API token.

## Kilo AI

```bash
# 1. Get your API key from https://kilo.ai
# 2. Configure (OpenAI-compatible):
```

```json
{
  "kilo": { "kind": "kilo", "model": "kilo-default", "apiKey": "..." }
}
```

Kilo provides hosted model inference with an OpenAI-compatible API.
